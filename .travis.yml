language: node_js
before_install: nvm install 8.11
install:
  - npm install
  - npm run tests

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN  # Set in the settings page of your repository, as a secure variable
  keep-history: true
  on:
    branch: master